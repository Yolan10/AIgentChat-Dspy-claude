{
  "name": "aigentchat-dspy",
  "version": "1.0.0",
  "description": "AI Agent Chat Simulation System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && pip install -r requirements.txt && mkdir -p logs templates",
    "start": "python api.py"
  },
  "engines": {
    "node": "18.x"
  }
}
